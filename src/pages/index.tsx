import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Blog with GraphQL</title>
      </Head>
    </div>
  );
};

export default Home;
